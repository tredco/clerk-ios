name: Checks

on:
  push:
    branches:
      - main
      - "tredco/nb-NO-*"

permissions:
  contents: read

jobs:
  checks:
    name: Shared checks
    permissions:
      actions: read
      checks: write
      contents: read
    uses: ./.github/workflows/shared-checks.yml
